import { HasCapabilityDirective } from './has-capability.directive';

describe('HasCapabilityDirective', () => {
  it('should create an instance', () => {
    // const directive = new HasCapabilityDirective();
    // expect(directive).toBeTruthy();
  });
});
