import { AddTabDirective } from './add-tab.directive';

describe('AddTabDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTabDirective();
    expect(directive).toBeTruthy();
  });
});
